apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "e-market.fullname" . }}-test-connection"
  labels:
    {{- include "e-market.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "e-market.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
